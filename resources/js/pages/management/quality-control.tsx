import WorkspacePage from '@/components/workspace-page';

export default function QualityControl() {
    return (
        <WorkspacePage
            title="Quality Control"
            description="Review quality checks and outstanding items."
        />
    );
}
