import WorkspacePage from '@/components/workspace-page';

export default function BookingBoard() {
    return (
        <WorkspacePage
            title="Booking Board"
            description="View scheduled bookings and appointments."
        />
    );
}
